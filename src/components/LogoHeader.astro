
<script>
  const btn = document.querySelector(".btn");
  btn.addEventListener("click", () => {
    btn.classList.toggle("active");
    const container = document.querySelector("#container");
    if (container.lastElementChild.innerText === "🌙") {
      container.lastElementChild.innerText = "☀️";
      return;
    }
    container.lastElementChild.innerText = "🌙";

  });
</script>

<div 
  data-aos="fade-in"
  class="container-logo-hero">
  <div class="ray"></div>
  <div id="container" class="btn">
    <p>👨🏻‍💻</p>
    <p>🧠</p>
    <p>✍🏻</p>
    <p>⌨️</p>
  </div>
  <div class="btn"></div>
</div>


<style>
  p {
    font-size: 1.3rem;
    width: min-content;
    margin: 0;
    justify-self: center;
    align-self: center;
  }

  #container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-auto-rows: auto;
    box-sizing: border-box;

    width: min-content;
    height: min-content;
    padding: .5rem;

    /* filter: drop-shadow(0px 4px 4px rgba(0, 0, 0, 0.5)); */

    background: linear-gradient(
      136.79deg,
      rgba(229, 148, 82, 1) -19.29%,
      rgba(28, 57, 105, 1) 86.47%
    );
    border-radius: 12px;
    mix-blend-mode: darken;
  }

  .container-logo-hero {
    cursor: default;
    position: absolute;

    z-index: 1;
    border-radius: 12px;
    padding: 0.2rem;

    background-image: linear-gradient(
        129.25deg,
        rgba(236, 236, 236, 0.7) 0%,
        rgba(0, 0, 0, 0) 40%
      ),
      linear-gradient(
        308.53deg,
        rgba(236, 236, 236, 0.7) 0%,
        rgba(0, 0, 0, 0) 45%
      );
    background-size: 400%;
    background-position: 300px;
    animation: move infinite 5s ease-in-out;
    /* box-shadow: 0px 4px 4px 0px rgba(0, 0, 0, 0.2); */
    transition: box-shadow 1s;
  }

  .container-logo-hero:hover {
    box-shadow: 0px 0px 0px 2px rgba(255, 255, 255, .3);
    background-image: linear-gradient(
        129.25deg,
        rgba(236, 236, 236, 1) 0%,
        rgba(0, 0, 0, 0) 40%
      ),
      linear-gradient(308.53deg, rgba(236, 236, 236, 1) 0%, rgba(0, 0, 0, 0) 45%);
  }

  @keyframes move {
    0% {
      background-position: 100px;
    }
    50% {
      background-position: 300px;
    }
    100% {
      background-position: 100px;
      ;
    }
  }
  @media screen and (max-width: 600px){
    .container-logo-hero {
      display: none;
    }
  }
</style>