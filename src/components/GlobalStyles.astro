<style is:global lang="scss">
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@700&display=swap');

  *{
    padding: 0;
    margin: 0;
    box-sizing: border-box;
  }
  html {
    background: #02010a !important;
  }

  @font-face {
    font-family: 'CaskaydiaCove';
    // src: url('/CaskaydiaCove[wght].ttf') format('truetype-variations');
    src: url('/caskaydiacovewght.woff') format('woff');
  }
  
  .active{
    background: rgba(2, 1, 10, 0.38);
    box-shadow: inset 0px -2px .1px #E59452;
    // border-bottom: 2px solid #E59452;
  }
  .highlight{
    background-image: linear-gradient(
        129.25deg,
        rgba(229, 148, 82, 1),
        rgba(70, 115, 189, 1)
      );
    background-clip: text;
    -webkit-background-clip: text;
    color: transparent;
    font-style: italic;
    padding-right: .4rem;
  }
  .normal{
    font-style: normal;
  }

  .perfil-img {
    z-index: 0;
    grid-column: 3/5;
    grid-row: 1/3;
    
    width: 20vw;
    width: clamp(50px, 20vw, 300px);
    
    max-width:400px;
    
    height: 20vw;
    height: clamp(50px, 20vw, 300px);
    
    max-height:400px;

    border-radius: 400px;
    justify-self: center;
    align-self: center;
    box-shadow: 0px 0px 100px rgba(0, 0, 0, .25);
    border: 5px solid rgba(255, 255, 255, .1);
    filter: drop-shadow(0px 4px 4px rgba(0, 0, 0, 0.25));
    border-radius: 200px;
  }

</style>
